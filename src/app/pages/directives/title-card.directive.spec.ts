import { TitleCardDirective } from './title-card.directive';

describe('TitleCardDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleCardDirective();
    expect(directive).toBeTruthy();
  });
});
